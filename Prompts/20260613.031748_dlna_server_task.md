# Task: DLNA/UPnP AV Media Server Implementation

- [x] Create `pom.xml` configuration with dependencies and Shade plugin
- [x] Create Logback configuration (`logback.xml`)
- [x] Implement utility classes:
  - [x] `MimeTypeResolver` for resolving MIME types and DLNA content formats
  - [x] `MetadataExtractor` for extracting media properties (size, name, mime)
- [x] Implement models:
  - [x] `MediaItem` representing audio, video, or image items
  - [x] `MediaContainer` representing directories or containers
  - [x] `MediaCatalog` for in-memory thread-safe storage of media content
- [x] Implement CLI and Filesystem Scanner:
  - [x] `CliParser` for parsing command-line parameters
  - [x] `MediaScanner` for recursive scanning of files
  - [x] `FileWatcher` for filesystem change detection and catalog updating
- [x] Implement network services:
  - [x] `HttpStreamingServer` supporting byte-range requests and buffered streaming
  - [x] `ConnectionManagerService` for connection setup and state
  - [x] `ContentDirectoryService` for browsing/searching items and rendering DIDL-Lite XML
  - [x] `DlnaMediaServer` for UPnP registry binding and Cling stack initialization
- [x] Implement entry point:
  - [x] `Application` orchestrating parser, scanner, watcher, streaming and DLNA services
- [x] Implement tests:
  - [x] Unit tests for `MimeTypeResolver`, `CliParser`, `HttpStreamingServer`, `MediaScanner`, and `ContentDirectoryService`
- [x] Verify execution and document build instructions in `README.md`
