# User Prompt
Create a complete Java CLI application that functions as a DLNA/UPnP AV Media Server.

Requirements:

1. Overview
- The application must expose a user-specified directory as a DLNA media library.
- DLNA-compatible devices (Smart TVs, VLC, Kodi, Windows Media Player, Android DLNA clients, etc.) should automatically discover the server on the local network.
- The application must be runnable entirely from the command line.
- The user launches it by passing a media folder path.

Example:

java -jar dlna-server.jar /path/to/media

or

dlna-server --media-dir /path/to/media

2. Technology Stack
- Language: Java 21+
- Build Tool: Maven
- Use a mature UPnP/DLNA Java library, preferably Cling.
- Use an embedded HTTP server (Jetty, Undertow, or Java HttpServer) for media streaming.
- No GUI.
- Cross-platform (Linux, macOS, Windows).

3. Functional Requirements

3.1 DLNA Discovery
- Implement SSDP discovery.
- Advertise the server as a UPnP MediaServer device.
- Broadcast presence on startup.
- Respond correctly to M-SEARCH requests.
- Devices on the same LAN should automatically discover it.

3.2 Content Directory Service
- Implement a ContentDirectory service.
- Allow clients to browse folders and files.
- Map filesystem folders to DLNA containers.
- Map media files to DLNA items.

3.3 Connection Manager Service
- Implement ConnectionManager service.
- Provide required UPnP responses for interoperability.

3.4 Media Scanning
- Recursively scan the supplied media directory.
- Detect:
  - mp4
  - mkv
  - avi
  - mov
  - mp3
  - flac
  - wav
  - m4a
  - aac
  - jpg
  - jpeg
  - png
- Ignore unsupported files.
- Build an in-memory media catalog.
- Preserve folder hierarchy.

3.5 Metadata
- Generate DIDL-Lite metadata.
- Include:
  - title
  - file size
  - mime type
  - duration when available
  - content URL
- Use sensible defaults when metadata is unavailable.

3.6 Streaming
- Stream media over HTTP.
- Support:
  - GET requests
  - HEAD requests
  - byte-range requests
  - seeking
- Large files must stream without loading entirely into memory.
- Use buffered streaming.

3.7 MIME Types
- Correctly serve:
  - video/mp4
  - video/x-matroska
  - video/x-msvideo
  - audio/mpeg
  - audio/flac
  - audio/wav
  - image/jpeg
  - image/png
- Allow easy extension.

3.8 Dynamic Updates
- Optional initial implementation:
  - manual rescan command
- Preferred:
  - filesystem watcher
  - automatically update catalog when files change

3.9 Logging
- Startup logs
- Discovery logs
- Browse requests
- Streaming requests
- Errors
- Use SLF4J + Logback.

4. CLI Requirements

Support:

--media-dir <path>
--name <server name>
--port <http port>
--help

Examples:

dlna-server \
  --media-dir /media/movies \
  --name "My DLNA Server" \
  --port 8200

Defaults:
- Name: Java DLNA Server
- Port: 8200

5. Project Structure

Use a clean architecture:

src/main/java/
  cli/
  discovery/
  dlna/
  content/
  streaming/
  metadata/
  filesystem/
  model/
  util/

Suggested classes:

Application
CliParser
MediaScanner
MediaCatalog
MediaItem
MediaContainer
DlnaMediaServer
ContentDirectoryService
ConnectionManagerService
HttpStreamingServer
MimeTypeResolver
MetadataExtractor
FileWatcher

6. Code Quality

- Production-quality code.
- Strong typing.
- Java records where appropriate.
- Proper exception handling.
- No hardcoded paths.
- Thread-safe where required.
- Javadoc for public APIs.
- Follow SOLID principles.
- Avoid code duplication.

7. Testing

Provide:
- Unit tests using JUnit 5.
- Tests for:
  - media scanning
  - MIME detection
  - metadata generation
  - DIDL-Lite generation
  - range request parsing
- Integration tests where feasible.

8. Deliverables

Generate:

- Complete Maven project.
- pom.xml with all dependencies.
- Full source code.
- Configuration files.
- Logback configuration.
- README.md.

README must include:
- build instructions
- run instructions
- DLNA compatibility notes
- troubleshooting
- architecture overview

9. Acceptance Criteria

After building and running:

mvn package

java -jar target/dlna-server.jar \
  --media-dir /path/to/media

The server must:

- Appear automatically on DLNA clients on the LAN.
- Allow browsing folders.
- Allow browsing media files.
- Stream audio/video content.
- Support seeking via byte-range requests.
- Handle large media files efficiently.
- Shut down cleanly.

Implement all code necessary to make the application functional. Do not provide pseudocode. Generate complete compilable source files and project structure.

# Implementation Plan - DLNA/UPnP AV Media Server

This plan outlines the design and files to be created to build the complete DLNA/UPnP Media Server in Java 21+ using Maven, Cling, and JDK `HttpServer`.

## User Review Required

> [!IMPORTANT]
> Since the original `4thline.org` Maven repository is decommissioned, we will use a workaround to allow `cling-core` and `cling-support` to resolve properly. We will declare the Maven Central repositories, adding JitPack as a repository, and using `cling-core`/`cling-support` from standard maven public proxy repositories, or configure a fallback using jitpack or Maven Central mirrors.
> Actually, we can define JitPack or the standard `4thline-repo` as `http://4thline.org/m2` (with modern Maven configuration handling or HTTP blocking workaround if needed).
> We'll also provide a Gradle `build.gradle` configuration as a backup/alternative since the workspace already contains Gradle configuration files.

> [!NOTE]
> We will use Java's built-in `com.sun.net.httpserver.HttpServer` for streaming. It is robust, thread-safe, has zero dependencies, supports chunked transfer encoding, and easily handles custom headers like `Range` and `Content-Range`.

## Open Questions

None at this stage. We will proceed with implementing a standard, clean, and highly robust architecture.

## Proposed Changes

### Build and Configuration Files

#### [NEW] [pom.xml](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/pom.xml)
Maven configuration containing dependencies: Cling Core, Cling Support, SLF4J, Logback, and JUnit 5. Configured with `maven-shade-plugin` or `maven-assembly-plugin` to package a executable fat jar.

#### [NEW] [logback.xml](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/resources/logback.xml)
Logback logging configuration.

### Source Code Package `com.vypeensoft.dlnaserver`

#### [NEW] [Application.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/Application.java)
Entry point. Parses arguments, setups logging, initializes components, starts the services, and registers shutdown hook.

#### [NEW] [CliParser.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/cli/CliParser.java)
Parses command line arguments (e.g. `--media-dir`, `--name`, `--port`, `--help`).

#### [NEW] [MediaItem.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/model/MediaItem.java)
Java record/class representing a media file, containing ID, title, file path, mime type, size, duration, and URL.

#### [NEW] [MediaContainer.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/model/MediaContainer.java)
Represents a folder in the hierarchy, containing sub-containers and media items.

#### [NEW] [MediaCatalog.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/content/MediaCatalog.java)
Thread-safe in-memory database of all media containers and items, mapping filesystem paths to UPnP container/item IDs.

#### [NEW] [MimeTypeResolver.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/util/MimeTypeResolver.java)
Utility to map file extensions to MIME types.

#### [NEW] [MetadataExtractor.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/metadata/MetadataExtractor.java)
Extracts file size, type, title, and attempts to extract duration (e.g. from MP3/MP4 headers if lightweight, otherwise using clean defaults).

#### [NEW] [MediaScanner.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/filesystem/MediaScanner.java)
Recursively scans filesystem directories, filters by allowed extensions, builds the catalog.

#### [NEW] [FileWatcher.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/filesystem/FileWatcher.java)
Filesystem watcher (using Java's `WatchService`) to automatically trigger incremental rescan/updates of the catalog when files are added, modified, or deleted.

#### [NEW] [HttpStreamingServer.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/streaming/HttpStreamingServer.java)
Lightweight HTTP streaming server supporting GET, HEAD, Range headers, Content-Range, seeking, and low-memory buffered chunks streaming.

#### [NEW] [DlnaMediaServer.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/dlna/DlnaMediaServer.java)
Binds UPnP Services (ContentDirectory, ConnectionManager) using Cling, registers the local device, and starts the UPnP service stack.

#### [NEW] [ContentDirectoryService.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/content/ContentDirectoryService.java)
Extends Cling's `AbstractContentDirectoryService` to process UPnP browse and search requests, returning matching DIDL-Lite XML responses constructed from the in-memory `MediaCatalog`.

#### [NEW] [ConnectionManagerService.java](file:///x:/Projects_X/0_Active/1_Java_Active/DLNA_Server_Java_GITHUB/src/main/java/com/vypeensoft/dlnaserver/dlna/ConnectionManagerService.java)
Extends Cling's `ConnectionManagerService` to manage UPnP connections and state.

### Verification Plan

#### Automated Tests
We will add Unit Tests:
- `MimeTypeResolverTest.java` - verifies resolution of supported file types.
- `CliParserTest.java` - verifies command-line argument parsing.
- `HttpStreamingServerTest.java` - verifies range-request parser logic and streaming headers.
- `MediaScannerTest.java` - verifies scanning and catalog creation.
- `ContentDirectoryServiceTest.java` - verifies DIDL-Lite generation.

#### Manual Verification
We will run:
```bash
mvn clean package
java -jar target/dlna-server.jar --media-dir <test-dir> --port 8200 --name "My Test Server"
```
And verify:
1. Discovery via VLC (Local Network -> Universal Plug 'n' Play).
2. Media playback, seeking, and catalog browsing.
3. Shutdown capability.
